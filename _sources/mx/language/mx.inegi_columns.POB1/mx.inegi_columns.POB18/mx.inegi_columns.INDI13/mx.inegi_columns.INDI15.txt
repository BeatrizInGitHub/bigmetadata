.. _mx.inegi_columns.INDI15:

Male population 5 or more years old who speak an indigenous language and do not speak Spanish
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------













.. tabs::



   
   .. tab:: {"tab_name": "Per Population 5 or more years old who speak an indigenous language and do not speak Spanish"}

      Update a field of a table with an estimate of "Male population 5 or more years old who speak an indigenous language and do not speak Spanish" per Population 5 or more years old who speak an indigenous language and do not speak Spanish for a polygon
      using a combination of weighting by area and Population 5 or more years old who speak an indigenous language and do not speak Spanish.

      .. code-block:: postgresql

          WITH data AS (
            SELECT cartodb_id AS id, the_geom FROM <table_to_update>
          ),
          meta AS (
            SELECT OBS_GetMeta(ST_SetSRID(ST_Extent(the_geom), 4326),
            '[{"numer_id": "mx.inegi_columns.INDI15",
              "denom_id": "mx.inegi_columns.INDI13",
              "normalization": "denominated"}]'
            , 1, 1) meta
          FROM data)
          UPDATE <table_to_update> SET <field_to_update> = (data->0->>'value')::Numeric
          FROM OBS_GetData(
                (SELECT ARRAY_AGG((the_geom, id)::geomval) FROM data),
                (SELECT meta FROM meta)) obs_data
          WHERE <table_to_update>.cartodb_id = obs_data.id

      Obtain an estimate of "Male population 5 or more years old who speak an indigenous language and do not speak Spanish" per Population 5 or more years old who speak an indigenous language and do not speak Spanish for a polygon
      using a combination of weighting by area and Population 5 or more years old who speak an indigenous language and do not speak Spanish.

      .. code-block:: postgresql

          WITH data AS (
            SELECT 1 AS id,
            ST_Buffer(ST_SetSRID(ST_Point(-99.17, 19.413), 4326), 0.01) the_geom
          ),
          meta AS (
            SELECT OBS_GetMeta(ST_SetSRID(ST_Extent(the_geom), 4326),
            '[{"numer_id": "mx.inegi_columns.INDI15",
               "denom_id": "mx.inegi_columns.INDI13",
               "normalization": "denominated"}]'
            , 1, 1) meta
          FROM data)
          SELECT id, (data->0->>'value')::Numeric AS male_speak_ind_language_no_spanish_age_5_more_rate_2010
          FROM OBS_GetData(
                 (SELECT ARRAY_AGG((the_geom, id)::geomval) FROM data),
                 (SELECT meta FROM meta))

   



   
   .. tab:: {"tab_name": "Per Male population 5 or more years old"}

      Update a field of a table with an estimate of "Male population 5 or more years old who speak an indigenous language and do not speak Spanish" per Male population 5 or more years old for a polygon
      using a combination of weighting by area and Male population 5 or more years old.

      .. code-block:: postgresql

          WITH data AS (
            SELECT cartodb_id AS id, the_geom FROM <table_to_update>
          ),
          meta AS (
            SELECT OBS_GetMeta(ST_SetSRID(ST_Extent(the_geom), 4326),
            '[{"numer_id": "mx.inegi_columns.INDI15",
              "denom_id": "mx.inegi_columns.POB74",
              "normalization": "denominated"}]'
            , 1, 1) meta
          FROM data)
          UPDATE <table_to_update> SET <field_to_update> = (data->0->>'value')::Numeric
          FROM OBS_GetData(
                (SELECT ARRAY_AGG((the_geom, id)::geomval) FROM data),
                (SELECT meta FROM meta)) obs_data
          WHERE <table_to_update>.cartodb_id = obs_data.id

      Obtain an estimate of "Male population 5 or more years old who speak an indigenous language and do not speak Spanish" per Male population 5 or more years old for a polygon
      using a combination of weighting by area and Male population 5 or more years old.

      .. code-block:: postgresql

          WITH data AS (
            SELECT 1 AS id,
            ST_Buffer(ST_SetSRID(ST_Point(-99.17, 19.413), 4326), 0.01) the_geom
          ),
          meta AS (
            SELECT OBS_GetMeta(ST_SetSRID(ST_Extent(the_geom), 4326),
            '[{"numer_id": "mx.inegi_columns.INDI15",
               "denom_id": "mx.inegi_columns.POB74",
               "normalization": "denominated"}]'
            , 1, 1) meta
          FROM data)
          SELECT id, (data->0->>'value')::Numeric AS male_speak_ind_language_no_spanish_age_5_more_rate_2010
          FROM OBS_GetData(
                 (SELECT ARRAY_AGG((the_geom, id)::geomval) FROM data),
                 (SELECT meta FROM meta))

   




   .. tab:: {"tab_name": "Per sq km"}

      Update a field of a table with an estimate of "Male population 5 or more years old who speak an indigenous language and do not speak Spanish" per square kilometer using area
      weighted interpolation.

      .. code-block:: postgresql

          WITH data AS (
            SELECT cartodb_id AS id, the_geom FROM <table_to_update>
          ),
          meta AS (
            SELECT OBS_GetMeta(ST_SetSRID(ST_Extent(the_geom), 4326),
            '[{"numer_id": "mx.inegi_columns.INDI15",
              "normalization": "areaNormalized"}]'
            , 1, 1) meta
          FROM data)
          UPDATE <table_to_update> SET <field_to_update> = (data->0->>'value')::Numeric
          FROM OBS_GetData(
                (SELECT ARRAY_AGG((the_geom, id)::geomval) FROM data),
                (SELECT meta FROM meta)) obs_data
          WHERE <table_to_update>.cartodb_id = obs_data.id

      Obtain an estimate of "Male population 5 or more years old who speak an indigenous language and do not speak Spanish" per square kilometer using area
      weighted interpolation.

      .. code-block:: postgresql

          WITH data AS (
            SELECT 1 AS id,
            ST_Buffer(ST_SetSRID(ST_Point(-99.17, 19.413), 4326), 0.01) the_geom
          ),
          meta AS (
            SELECT OBS_GetMeta(ST_SetSRID(ST_Extent(the_geom), 4326),
            '[{"numer_id": "mx.inegi_columns.INDI15",
               "normalization": "areaNormalized"}]'
            , 1, 1) meta
          FROM data)
          SELECT id, (data->0->>'value')::Numeric AS male_speak_ind_language_no_spanish_age_5_more_per_sq_km_2010
          FROM OBS_GetData(
                 (SELECT ARRAY_AGG((the_geom, id)::geomval) FROM data),
                 (SELECT meta FROM meta))

   .. tab:: {"tab_name": "Raw count"}

      Update a field of a table with an estimate of "Male population 5 or more years old who speak an indigenous language and do not speak Spanish" for a polygon using area-weighted
      interpolation.

      .. code-block:: postgresql

          WITH data AS (
            SELECT cartodb_id AS id, the_geom FROM <table_to_update>
          ),
          meta AS (
            SELECT OBS_GetMeta(ST_SetSRID(ST_Extent(the_geom), 4326),
            '[{"numer_id": "mx.inegi_columns.INDI15",
              "normalization": "predenominated"}]'
            , 1, 1) meta
          FROM data)
          UPDATE <table_to_update> SET <field_to_update> = (data->0->>'value')::Numeric
          FROM OBS_GetData(
                (SELECT ARRAY_AGG((the_geom, id)::geomval) FROM data),
                (SELECT meta FROM meta)) obs_data
          WHERE <table_to_update>.cartodb_id = obs_data.id

      Obtain an estimate of "Male population 5 or more years old who speak an indigenous language and do not speak Spanish" for a polygon using area-weighted
      interpolation.

      .. code-block:: postgresql

          WITH data AS (
            SELECT 1 AS id,
            ST_Buffer(ST_SetSRID(ST_Point(-99.17, 19.413), 4326), 0.01) the_geom
          ),
          meta AS (
            SELECT OBS_GetMeta(ST_SetSRID(ST_Extent(the_geom), 4326),
            '[{"numer_id": "mx.inegi_columns.INDI15",
               "normalization": "predenominated"}]'
            , 1, 1) meta
          FROM data)
          SELECT id, (data->0->>'value')::Numeric AS male_speak_ind_language_no_spanish_age_5_more_2010
          FROM OBS_GetData(
                 (SELECT ARRAY_AGG((the_geom, id)::geomval) FROM data),
                 (SELECT meta FROM meta))









Boundaries
~~~~~~~~~~

.. raw:: html

  <table border="1" class="docutils">
  <thead valign="bottom">
  <tr class="row-odd"><th class="head">Boundary</th>
  <th class="head">Time (default is latest)</th>
  </tr>
  </thead>
  <tbody valign="top">


  
.. raw:: html

  <tr class="row-even"><td>

:ref:`mx.inegi.municipio`

.. raw:: html

  </td>
  <td>

2010

.. raw:: html

  </td>

  
.. raw:: html

  <tr class="row-even"><td>

:ref:`mx.inegi.ageb`

.. raw:: html

  </td>
  <td>

2010

.. raw:: html

  </td>

  
.. raw:: html

  <tr class="row-even"><td>

:ref:`mx.inegi.localidad_urbana_y_rural_amanzanada`

.. raw:: html

  </td>
  <td>

2010

.. raw:: html

  </td>

  
.. raw:: html

  <tr class="row-even"><td>

:ref:`mx.inegi.entidad`

.. raw:: html

  </td>
  <td>

2010

.. raw:: html

  </td>

  

.. raw:: html

  </tr>
  </tbody>
  </table>




Related columns
~~~~~~~~~~~~~~~
    
      * :ref:`mx.inegi_columns.INDI13`
    
      * :ref:`mx.inegi_columns.POB74`
    



Source
~~~~~~
    
      * :ref:`mx.inegi_columns.inegi-source`
    



License
~~~~~~~
    
      * :ref:`mx.inegi_columns.inegi-license`
    